Orchestrator: GenerateExperience.call(topic_id:, experience_generation_intent:)

- Prompt 1: GenerateExperienceBasics
  - Ruby Input:
    - topic_id
    - experience_generation_intent (for the prompt, not the end-user)
  - LLM Input: generate_experience_basics
    - topic_name
    - topic_description
    - experience_generation_intent
  - Output:
      - name
      - description
      - generation_intent (send a more in-depth one back -- use original as jumping off point)
  - Persistence:
    - Experience.new(topic: topic, name: name, description: description, generation_intent: generation_intent).save!

- Prompt 2: GenerateConversationLists
    - LLM Input:
        - topic_name
        - topic_description
        - experience_name
        - experience_description
        - experience_generation_intent

    - Output
        - lists array // just the metadata to create them in a follow-up prompt
          - name
          - description
          - generation_intent (for future conversation)

- Prompt 3: GenerateConversationListElements (repeat for each list type)
    - LLM Input
        - topic_name
        - topic_description
        - experience_name
        - experience_description
        - experience_generation_intent
        - list_name
        - list_description
        - list_generation_intent
        - other_list_names
        - number_of_elements -- @available_lengths = [3,4,5,6]; @available_lengths.shuffle.pop; reinit if necessary -- randomize the lengths across page

- Populate experience...
  - GenerateDesignReviews (+ maybe one more for the details? just one? pass existing design review generation_intents?)
  - GenerateIncidents


- GenerateDesignReview
    - LLM Input
        - existing_design_review_generation_intents

    "Give me another component. I want it to be a "design review" where the only option is a free-form text box (around 2-3 lines) to provide feedback on a paragraph or two from a fictitious teamate (use "/profile-photo-1.jpg" as the image path for the teammate's avatar).
     It should feel realistic. Layered idea that incorporates leader election somehow. Shouldn't use the phrase "Raft" explicitly. But somebody who finished this entire page should be able to find a fatal flaw. It should sound like a valid design, but fail due to a commonly missed

- CacheLlmResponseForExperience(experience_id:)