<div class="lg:max-w-3xl mx-auto py-10">
  <h1 class="text-2xl font-semibold mb-4">Unsubscribe from SEV-1 emails?</h1>

  <p class="mb-2">
    <span class="font-semibold"><%= @member.email %></span> will stop receiving messages from Replicate.
  </p>

  <div class="mt-6 flex items-center gap-3">
    <%= form_with url: "/members/#{@member.id}/unsubscribe", method: :post, local: true do %>
      <%= submit_tag "Yes, unsubscribe", class: "px-4 py-2 rounded bg-red-600 text-white font-semibold" %>
    <% end %>

    <%= link_to "Cancel", root_path, class: "px-4 py-2 rounded border font-semibold" %>
  </div>

  <p class="mt-6 text-sm text-gray-500">
    Change your mind later? Simply email
    <a href="mailto:support@replicate.info" class="text-blue-600 hover:underline font-semibold">support@replicate.info</a> to resubscribe.
  </p>
</div>