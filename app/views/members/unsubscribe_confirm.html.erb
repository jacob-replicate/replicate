<div class="max-w-3xl mx-auto py-10">
  <h1 class="text-2xl font-semibold mb-4">You have unsubscribed.</h1>

  <span class="font-semibold"><%= @member.email %></span> will no longer receive emails. <a href="/members/<%= @member.id %>/resubscribe" class="text-blue-600 hover:underline font-semibold">Click here</a> if you would like to resubscribe.
</div>