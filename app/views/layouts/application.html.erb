<!DOCTYPE html>
<html>
  <head>
    <title><%= @title || "replicate.info" %></title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta charset="utf-8">
    <meta name="description" content="replicate.info helps early-stage engineering teams prevent their next postmortem, without lifting a finger.">
    <meta name="keywords" content="engineering team insights, developer check-ins, leadership reports, skill growth, blind spot detection">
    <meta property="og:url" content="<%= request.original_url %>">
    <meta property="og:image" content="/hero.jpg">
    <meta property="og:description" content="replicate.info helps early-stage engineering teams prevent their next postmortem, without lifting a finger.">
    <meta property="og:title" content="<%= @title || 'replicate.info' %>">
    <meta property="og:site_name" content="<%= @title || 'replicate.info' %>">
    <meta property="og:see_also" content="https://replicate.info">
    <meta property="twitter:image" content="/hero.jpg">
    <meta property="twitter:description" content="replicate.info helps early-stage engineering teams prevent their next postmortem, without lifting a finger.">
    <meta property="twitter:title" content="<%= @title || 'replicate.info' %>">
    <meta property="twitter:url" content="<%= request.original_url %>">
    <meta name="msapplication-TileColor" content="#4d5562">
    <meta name="theme-color" content="#4d5562">

    <link rel="canonical" href="<%= request.original_url %>">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Source+Code+Pro:ital,wght@0,200..900;1,200..900&display=swap" rel="stylesheet">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">
    <link rel="mask-icon" href="/safari-pinned-tab.svg" color="#5bbad5">
    <link rel="preload" href="/fonts/SuisseIntl-Regular.woff2" as="font" type="font/woff2" crossorigin="anonymous">
    <link rel="preload" href="/fonts/SuisseIntl-Semibold.woff2" as="font" type="font/woff2" crossorigin="anonymous">
    <link rel="preload" href="/fonts/SuisseIntl-Bold.woff2" as="font" type="font/woff2" crossorigin="anonymous">
    <%= stylesheet_link_tag "application", as: "style" %>
    <%= stylesheet_link_tag "tailwind", as: "style" %>
    <%= javascript_include_tag "application", defer: true %>
  </head>

  <body>
    <% if params[:controller] == "static" && params[:action] == "index" %>
      <div class="container mx-auto">
        <div class="px-4 py-4 md:py-6 lg:px-[20px] xl:px-[120px] 2xl:px-[280px]">
          <%= render 'layouts/navbar' %>

          <div class="pt-4 md:pt-0 lg:pt-2">
            <%= yield %>
          </div>
        </div>
      </div>
    <% else %>
      <div class="h-screen flex flex-col bg-white container mx-auto">
        <div class="px-4 xl:px-56 2xl:px-[320px] py-4 md:py-6">
          <div class="max-w-3xl mx-auto"><%= render 'layouts/navbar' %></div>

          <div class="max-w-3xl mx-auto">
            <%= yield %>
          </div>
        </div>
      </div>
    <% end %>
  </body>
</html>