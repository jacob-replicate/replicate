<!DOCTYPE html>
<html>
  <head>
    <script>
      // Prevent flash: apply theme before render
      (function() {
        const stored = localStorage.getItem('theme');
        const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        if (stored === 'dark' || (!stored && prefersDark)) {
          document.documentElement.classList.add('dark');
        }
      })();
    </script>
    <title><%= @title || "invariant.training" %></title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="keywords" content="engineering team coaching, developer coaching insights, technical leadership, engineering blind spots, team performance">
    <meta name="theme-color" content="#4d5562">
    <meta name="msapplication-TileColor" content="#4d5562">
    <meta property="og:url" content="<%= request.original_url %>">
    <meta property="og:type" content="website">
    <meta property="og:title" content="<%= @title || 'invariant.training - Prevent your next SEV-1' %>">
    <meta property="og:description" content="">
    <meta property="og:image" content="https://invariant.training/social.png">
    <meta property="og:site_name" content="invariant.training">
    <meta property="og:locale" content="en_US">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:url" content="<%= request.original_url %>">
    <meta name="twitter:title" content="<%= @title || 'invariant.training - Prevent your next SEV-1' %>">
    <meta name="twitter:description" content="">
    <meta name="twitter:image" content="https://invariant.training/social.png">

    <link rel="canonical" href="<%= request.original_url %>">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Source+Code+Pro:ital,wght@0,200..900;1,200..900&display=swap" rel="stylesheet">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">
    <link rel="mask-icon" href="/safari-pinned-tab.svg" color="#5bbad5">
    <link rel="preload" href="/fonts/SuisseIntl-Regular.woff2" as="font" type="font/woff2" crossorigin="anonymous">
    <link rel="preload" href="/fonts/SuisseIntl-Semibold.woff2" as="font" type="font/woff2" crossorigin="anonymous">
    <link rel="preload" href="/fonts/SuisseIntl-Bold.woff2" as="font" type="font/woff2" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Source+Code+Pro:ital@0;1&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "application", as: "style" %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.11.1/styles/vs2015.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.11.1/highlight.min.js"></script>
    <script src="//cdnjs.cloudflare.com/ajax/libs/highlightjs-line-numbers.js/2.9.0/highlightjs-line-numbers.min.js"></script>
    <script src="/terraform.js"></script>
    <script>
      document.addEventListener('DOMContentLoaded', () => {
        hljs.registerLanguage('terraform', hljsDefineTerraform);
        hljs.registerLanguage('hcl', hljsDefineTerraform);
        hljs.highlightAll();
        hljs.initLineNumbersOnLoad();
      });
    </script>
    <%= stylesheet_link_tag "tailwind", as: "style" %>
    <script>window.isAdmin = <%= admin? %>;</script>
    <%= javascript_include_tag "application", defer: true %>
    <script src="https://cdn.jsdelivr.net/npm/echarts@latest/dist/echarts.min.js"></script>
    <%= Sentry.get_trace_propagation_meta.html_safe %>
  </head>

  <body class="bg-[#fbfbfb] dark:bg-zinc-900">
    <% if (params[:controller] == "static" && params[:action] == "index") || params[:action] == "growth" || params[:action] == "show" %>
      <div class="container mx-auto">
        <div class="px-4 pb-6 pt-4 md:pt-6 md:pb-8 md:px-0 lg:px-[125px] xl:px-[248px] 2xl:px-[385px]">
          <%= render 'layouts/navbar' %>

          <div class="pt-4 md:pt-0 lg:pt-2">
            <% if flash[:notice] %>
              <div class="mb-4 px-4 py-3 rounded-md bg-emerald-50 dark:bg-emerald-900/30 border border-emerald-200 dark:border-emerald-800 text-emerald-700 dark:text-emerald-300 text-sm">
                <%= flash[:notice] %>
              </div>
            <% end %>
            <% if flash[:alert] %>
              <div class="mb-4 px-4 py-3 rounded-md bg-red-50 dark:bg-red-900/30 border border-red-200 dark:border-red-800 text-red-700 dark:text-red-300 text-sm">
                <%= flash[:alert] %>
              </div>
            <% end %>
            <%= yield %>
          </div>
        </div>
      </div>
    <% else %>
      <div class="h-screen flex flex-col container mx-auto">
        <div class="px-4 xl:px-56 2xl:px-[320px] py-4 md:py-6">
          <div class="max-w-3xl mx-auto"><%= render 'layouts/navbar' %></div>

          <div class="max-w-3xl mx-auto">
            <%= yield %>
          </div>
        </div>
      </div>
    <% end %>
  </body>
</html>