<%# MOCKUP: Hardcoded intro card for phantom-nxdomains experience %>
<% if @experience.code == "phantom-nxdomains" %>
  <div class="mt-6 mb-8 bg-white dark:bg-slate-900 rounded-xl shadow-sm border border-slate-100 dark:border-slate-800 p-6">
    <div class="flex items-center gap-3 mb-3">
      <span class="text-xs font-medium text-slate-500 dark:text-slate-400">25 min</span>
      <span class="w-1 h-1 rounded-full bg-slate-300 dark:bg-slate-600"></span>
      <span class="text-xs font-medium text-slate-500 dark:text-slate-400">16 challenges</span>
    </div>
    <h1 class="text-xl md:text-2xl font-bold text-slate-900 dark:text-white tracking-tight mb-3"><%= @experience.name %></h1>

    <div class="text-sm text-slate-600 dark:text-slate-400 leading-relaxed">
      <p class="mb-4">
        DNS caches lie. When you update a record, some resolvers see the new value while others insist it doesn't exist—returning NXDOMAIN for a domain that's clearly alive. This "phantom" effect causes mysterious outages during migrations. Explore the mechanics below, or ask anything to go deeper.
      </p>

      <div class="flex gap-2">
        <input type="text" placeholder="Ask a question..." class="flex-1 px-3 py-2 text-sm rounded-md border border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-800 text-slate-900 dark:text-white placeholder-slate-400 focus:outline-none focus:ring-2 focus:ring-sky-500 focus:border-transparent">
        <button class="px-3 py-2 text-sm bg-sky-500 hover:bg-sky-600 text-white font-medium rounded-md transition-colors">Send</button>
      </div>
    </div>
  </div>
<% end %>

<% @experience.elements.root_level.includes(elements: :conversation).order(sort_order: :asc).each_with_index do |element, i| %>
  <%= render "elements/#{element.code.underscore}", element: element %>
<% end %>