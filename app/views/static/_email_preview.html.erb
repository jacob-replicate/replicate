<% next_monday = Date.today.next_occurring(:monday).strftime("%b %-d") %>
<div class="bg-white dark:bg-zinc-800/50 border border-zinc-200 dark:border-zinc-700 rounded-md shadow-sm overflow-hidden text-sm">
  <div class="bg-zinc-700 dark:bg-zinc-700 text-white px-3 py-2 border-b border-zinc-600 dark:border-zinc-600 font-medium flex justify-between items-center">
    <span>Re: The GC job that looked safe</span>
    <span class="text-xs text-zinc-400 font-normal">Mon, <%= next_monday %> &bull; 1:17pm</span>
  </div>

  <div class="text-zinc-700 dark:text-zinc-200">
    <div class="p-3">
      <div>
        <div class="font-semibold text-zinc-500 dark:text-zinc-400">Replicate</div> A job starts garbage collecting a soft-deleted <span class="font-semibold">Organization</span> with 400k dependent records, all in one transaction. <span class="font-semibold">pg_stat_slru</span> shows 87% usage on <span class="font-semibold">MultiXactOffset</span>. What do you want to rule out early?
      </div>
    </div>
    <div class="p-3 border-t border-zinc-100 dark:border-zinc-700">
      <div>
        <div class="font-semibold text-zinc-500 dark:text-zinc-400">Alex Shaw</div> i'm not sure what that is, but I think we had something similar when deleting old customer records. the site got slow, so we killed the job and just looped through deletes in the console.
      </div>
    </div>
    <div class="p-3 border-t border-zinc-100 dark:border-zinc-700">
      <div>
        <div class="font-semibold text-zinc-500 dark:text-zinc-400">Replicate</div> That works until high-fanout deletes grab locks for FK checks, and SLRU exhaustion stalls reads/writes across the site. Let's try again.
      </div>
    </div>
  </div>
</div>