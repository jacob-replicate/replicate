- Make a new Apple ID for IBM phone so you can keep texting yourself
- Include relevant links in prompts if needed
- Auto-focus the input box (on desktop at least)
- Fix scrolling + viewport height issue in chat view
- Set up email (accounts + webhooks)
- Wire up email invitation flow
- Wire up the weekly check-in flow
- Wire up the weekly manager report flow
- Mobile header links
- Weird border on conversation input box - left side gets cut off
- Stripe Atlas

Eventually:
- Handle page refreshes while streaming text during demo.
- Tighten up Terms + Privacy + Security + Features docs
- Instead of linking to Google searches, just generate your own content, and link to it (to keep emails tight).
    - You could also just go super in-depth in the emails themselves too (if they ask for it).