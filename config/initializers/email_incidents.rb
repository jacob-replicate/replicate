EMAIL_INCIDENTS = [
  {
    description: "SSO callback responses weren't validated — users were logged in as admins.",
    subjects: [
      "SSO callback granted admin access without validation."
    ]
  }
]